[package]
name = "glowd"
version = "0.1.0"
edition = "2021"
description = "Tiny self-hosted web controller for MagicHome LED strips"
license = "MIT"

[dependencies]
tiny_http = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
